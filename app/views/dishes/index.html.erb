<div class="page-header">
  <h1>
    Dishes
    <a href="/dishes/new" class="btn btn-lg btn-success">New Dish</a>
  </h1>
</div>

<div class="row">
  <div class="col-md-12">
    <table class="table table-striped table-hover">
      <tr>
        <th>Type of food</th>
        <th>Restaurant location</th>
        <th>Dish details</th>
        <th>Cuisine</th>
        <th>Actions</th>
      </tr>

      <% @dishes.each do |dish| %>
      <tr>
        <td><%= dish.type_of_food %></td>
        <td><%= dish.restaurant_location %></td>
        <td><%= dish.dish_details %></td>
        <td><%= dish.cuisine_id %></td>
        <td>
          <a href="/dishes/<%= dish.id %>" class="btn btn-primary">Show</a>
          <a href="/dishes/<%= dish.id %>/edit" class="btn btn-warning">Edit</a>
          <a href="/delete_dish/<%= dish.id %>" class="btn btn-danger" rel="nofollow">Delete</a>
        </td>
      </tr>
      <% end %>
    </table>
  </div>
</div>
